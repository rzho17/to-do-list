import { setCompletion } from "./create-to-do";
import {
  homeTasks,
  initialProjectLoad,
  initialLoad,
  openModalBtns,
  openModals,
  displayDetails,
} from "./dom-manipulation";

initialLoad();
homeTasks();
initialProjectLoad();
// openModalBtns();
openModals();

// displayDetails();
