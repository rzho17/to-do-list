import { setCompletion } from "./create-to-do";
import {
  homeTasks,
  initialProjectLoad,
  initialLoad,
  openModalBtns,
  openModals,
  displayDetails,
  displayNav,
  navActive,
} from "./dom-manipulation";

initialLoad();
homeTasks();
initialProjectLoad();
// openModalBtns();
openModals();

// displayDetails();
